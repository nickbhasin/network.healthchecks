---
- name: Parse CPU health check output for EO
  ansible.utils.cli_parse:
    command: "show processes top once"
    parser:
      name: ansible.netcommon.native
    set_fact: cpu_health

- name: Debug CPU health check output
  ansible.builtin.debug:
    var: cpu_health
